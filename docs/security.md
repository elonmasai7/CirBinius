# Security Model

Security baselines:

- sandbox compilation and proving workloads
- path traversal and zip-bomb protections
- strict artifact hashing and provenance
- dependency lockfiles and supply-chain checks
- per-job CPU, memory, and timeout limits
- authenticated API access and rate limits

Threat model and control matrix will be expanded as API worker execution is implemented.
